pip install tensorflow
pip install pillow
pip install scipy
pip install pygame
pip install numpy
pip install albumentations opencv-python

# Only once:
brew install git-lfs         # if you are macOS (Homebrew)
choco install git-lfs        # if you are Windows (Chocolatey)
sudo apt install git-lfs     # if you are Ubuntu/Debian

git lfs install


reminder: please download all the files at one time by git clone the repo directly, rather than 
downloading the raw files from github repo, because one of these files, the model file, is very big.)






